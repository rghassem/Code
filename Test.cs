using UnityEngine;
using System.Collections;

public class Test : MonoBehaviour {

	// Use this for initialization
	/*void Start () {
		float metersPerPixel = Camera.main.fieldOfView / Screen.height;
		float theta = metersPerPixel;
		float diameter = collider.bounds.extents.x * 2;
		
		float diameterOverDistance = metersPerPixel;
		//diameter / distance = metersPerPixel;
		//float +
		//	= metersPerPixel / diameter;
		
		Transform cameraTransform = Camera.main.transform;
		transform.position = cameraTransform.position + (cameraTransform.forward * distance); 
	}
	
	// Update is called once per frame
	void Update () {
	
	}*/
}
